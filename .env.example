# Ideation-Claude Environment Variables
# Copy this file to .env and fill in your actual API keys
# 
# cp .env.example .env
# Then edit .env with your keys

# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Mem0 API key for cloud memory storage
# If not provided, will use local Qdrant storage
MEM0_API_KEY=your_mem0_api_key_here

# Optional: OpenAI API key for local Mem0 embeddings
# Only needed if using local Mem0 mode (when MEM0_API_KEY is not set)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Mem0 user ID for memory storage
# Defaults to "ideation_claude" if not set
MEM0_USER_ID=ideation_claude
